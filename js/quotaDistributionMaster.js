var quotaDistributionMaster = (function () {
	/**
	 * cacheDOM
	 */
	var $page = $('#pageContent');

	/**
	 * bind event
	 */
	
	/**
	 * init
	 */
	// show bache only
	$page.find('.masterOnly').removeClass('hide');

})();
